.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install cax

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cax
    $ pip install cax
